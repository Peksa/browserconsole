# Home page
GET		/                                   	    Application.index
GET		/{token}									Application.get
POST	/{token}/commands							Application.postCommand
POST	/{token}/responses							Application.postResponse


# Map static resources from the /app/public folder to the /public path
GET		/public/									staticDir:public
