# Application dependencies

require:
    - play 1.2.7
    - local-jar -> hazelcast 2.4

repositories:
    - local-jar:
        type: local
        artifact: "${application.path}/jar/[module]-[revision].jar"
        contains:
           - local-jar -> *